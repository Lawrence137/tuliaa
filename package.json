{
  "name": "tulia",
  "private": true,
  "workspaces": [
    "apps/*",
    "shared/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/user",
    "dev:user": "npm run dev --workspace=apps/user",
    "dev:counselor": "npm run dev --workspace=apps/counselor",
    "dev:institution": "npm run dev --workspace=apps/institution",
    "build": "npm run build --workspaces",
    "build:user": "npm run build --workspace=apps/user",
    "build:counselor": "npm run build --workspace=apps/counselor",
    "build:institution": "npm run build --workspace=apps/institution"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.17",
    "tailwindcss": "^4.1.17",
    "vite": "^7.2.4"
  }
}
